{
  "name": "agentops-frontend",
  "version": "5.4.0",
  "private": true,
  "type": "module",
  "description": "agentops — AI agentic telephony frontend (Vite + vanilla JS design system).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
